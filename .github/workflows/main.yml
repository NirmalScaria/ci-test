name: Continuous Integration
on:
  push:
    branches:
    - main
  pull_request:
  workflow_dispatch:
jobs:
  make-zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: make_zip
        run: zip -r main.zip /
